// OWNER: D2.  Product catalogue (screen 16 list, screen 4 product picker).
// Phase 0 stub — created so this file is never created twice. See OWNERSHIP.md.
//
// Both D3 (screen 16) and D1 (the quotation builder's picker) read this.
import { fail } from '@/lib/api'

export const runtime = 'nodejs'

export async function GET() {
  return fail('Not implemented yet — owner: D2', 501)
}
