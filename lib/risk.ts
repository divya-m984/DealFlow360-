// OWNER: D1.  scoreQuotation() — blended risk score + band + requires_manager/finance. Reads approval_policy; NEVER hardcode thresholds.
// Phase 0 stub — created so this file is never created twice. See OWNERSHIP.md.
export {}
