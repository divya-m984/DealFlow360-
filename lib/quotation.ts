// OWNER: D1.  recomputeQuotation() — bumps version if commercial terms changed, rescores, rewrites totals. Called from every mutating path.
// Phase 0 stub — created so this file is never created twice. See OWNERSHIP.md.
export {}
