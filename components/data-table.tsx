// OWNER: D3.  The single highest-leverage component in the project — seven
// list screens are built on it.  Start from shadcn's data-table recipe
// (TanStack useReactTable): column defs in, sorting, filter input, pagination,
// onRowClick, loading skeleton.
//
// Every list screen in the mockup says "Click a row to open…" — row click is
// the primary navigation mechanism of the whole app.
//
// Phase 0 stub.
export {}
